# Server
PORT=5001
NODE_ENV=development
CLIENT_URL=http://localhost:3000

# Optional: higher GitHub API rate limits for /api/v1/github/preview
GITHUB_TOKEN=

# Database & auth (OAuth, saved portfolios — not required for preview-only)
DATABASE_URL=
JWT_SECRET=
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
